name: django-oscar-elasticsearch

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: ['3.11']
        django-version: ['4.2']
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install .[test]
    - name: Run Elasticsearch
    # You may pin to the exact commit or the version.
    # uses: JonasGe/elasticsearch-with-plugins-action@9499e5e1943fe7869b7e6be9a45505afe5d387ea
      uses: JonasGe/elasticsearch-with-plugins-action@0.1.4
      with:
        # The version of the Elastic Stack you want to run
        stack-version: '6.8.2'
    - name: Run Tests
      run: |
        python manage.py test
